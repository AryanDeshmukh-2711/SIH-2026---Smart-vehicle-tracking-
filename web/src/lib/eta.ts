/**
 * ETA confidence rules live in `@himgati/shared`. The API is authoritative for
 * *computing* confidence; the client keeps these for formatting and for the
 * offline fallback path.
 */
export * from '@himgati/shared/eta';
