/**
 * ETA confidence rules live in `@routify/shared`. The API is authoritative for
 * *computing* confidence; the client keeps these for formatting and for the
 * offline fallback path.
 */
export * from '@routify/shared/eta';
