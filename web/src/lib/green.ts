/**
 * Green Score and CO₂ accounting now live in `@himgati/shared` — the API scores
 * vehicles with the identical function, so a score shown here and a score stored
 * server-side can never disagree.
 */
export * from '@himgati/shared/green';
