/**
 * Green Score and CO₂ accounting live in `@himgati/shared` — the API scores
 * vehicles with the identical function, so a score shown here and one stored
 * server-side can never disagree.
 */
export * from '@himgati/shared/green';
