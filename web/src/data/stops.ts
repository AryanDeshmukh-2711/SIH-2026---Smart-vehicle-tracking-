/**
 * The stop table now lives in `@himgati/shared/data` — the API seeds Postgres
 * from the same records, so a stop id here always resolves server-side too.
 */
export * from '@himgati/shared/data';
